============
Installation
============

At the command line::

    $ easy_install xponentialy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv xponentialy
    $ pip install xponentialy